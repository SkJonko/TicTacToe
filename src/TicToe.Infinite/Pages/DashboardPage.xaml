<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
	x:Class="TicToe.Infinite.Pages.DashboardPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:chart="clr-namespace:Syncfusion.Maui.Toolkit.Charts;assembly=Syncfusion.Maui.Toolkit"
	xmlns:controls="clr-namespace:TicToe.Infinite.Pages.Controls"
	xmlns:pageModels="clr-namespace:TicToe.Infinite.PageModels"
	xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
	xmlns:shimmer="clr-namespace:Syncfusion.Maui.Toolkit.Shimmer;assembly=Syncfusion.Maui.Toolkit"
	xmlns:strings="clr-namespace:TicToe.Infinite.Resources.Strings"
	xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
	x:Name="DashboardPageName"
	x:DataType="pageModels:DashboardPageModel">

	<ContentPage.Behaviors>
		<toolkit:EventToCommandBehavior
			BindingContext="{Binding Path=BindingContext, Source={x:Reference DashboardPageName}, x:DataType=ContentPage}"
			Command="{Binding AppearingCommand}"
			EventName="Appearing" />
	</ContentPage.Behaviors>


	<pullToRefresh:SfPullToRefresh>
		<pullToRefresh:SfPullToRefresh.PullableContent>
			<ScrollView>
				<Grid Padding="{StaticResource LayoutPadding}" RowSpacing="{StaticResource LayoutSpacing}">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<Label
						Grid.Row="0"
						SemanticProperties.HeadingLevel="Level1"
						Style="{StaticResource Title2}">
						<Label.FormattedText>
							<FormattedString>
								<Span Text="{x:Static strings:AppResources.DashboardPage_TotalGames}" />
								<Span Text="{Binding TotalGames}" />
							</FormattedString>
						</Label.FormattedText>
					</Label>

					<controls:GameChart Grid.Row="1" />

					<controls:MovesChart Grid.Row="2" />
				</Grid>
			</ScrollView>
		</pullToRefresh:SfPullToRefresh.PullableContent>
	</pullToRefresh:SfPullToRefresh>

</ContentPage>
